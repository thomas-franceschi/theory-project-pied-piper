# CSE 30151: Theory of Computing

Course Project, Spring 2017

The `bin.linux` and `bin.macos` directories contain binaries compiled
on Linux (student00.cse.nd.edu) and macOS 10.12.1,
respectively. You'll need to symlink one of them to `bin` in order to
run the tests.


