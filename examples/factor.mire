(:#)111*(:#)111*((:#)111*)*; // partition string into 2+ partitions of 2+ symbols each
{(x*(#1:x#)1*)*};            // x-out all partitions at same speed
((x:1)*#)*                   // check that all partitions were the same size
